rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporarily allow all reads and writes to debug connection issues
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
